# 📈 0050 量化交易戰情室 (Quant Trading Platform)

這是一個針對台股（以 0050 為主）設計的全方位量化交易系統。整合了 **歷史回測**、**模擬交易 (Paper Trading)**、**策略競技場** 以及 **桌面監控戰情室**。

系統能夠自動修復 Yahoo Finance 的歷史股價錯誤（拆股還原），並提供即時的視覺化報表與 Email 訊號通知。

## ✨ 功能特色

### 1. 🖥️ 桌面監控戰情室 (`gui_monitor.py`)
- **單一介面管理**：同時監控個人策略與後台排程。
- **每日自動排程**：內建排程器，每日 13:40 自動執行掃描。
- **即時訊號通知**：偵測黃金/死亡交叉，並發送 Email 通知。
- **五個口袋模擬**：同時模擬 1千、1萬、10萬、50萬、100萬本金的交易績效（含手續費與稅金）。

### 2. 🏆 策略競技場 (`monitor_top10.py`)
- **Top 10 策略對決**：每日自動運算 10 組不同參數的 MACD 策略。
- **績效排行榜**：自動將每日戰績寫入資料庫，並在網頁前端顯示即時排名。
- **汰弱留強**：透過長期數據找出最適合當前盤勢的策略參數。

### 3. 📊 網頁回測中心 (Web Dashboard)
- **視覺化圖表**：整合 ECharts，繪製股價與資產成長曲線。
- **強效資料修復**：獨家演算法，自動修復 0050 在 2014 年與 2024 年的拆股/還原價錯誤。
- **互動式參數調整**：可在網頁上即時調整 MACD/KD 參數進行回測。
- **詳細交易紀錄**：列出每一筆買賣的時間、價格與成本。

### 4. 💰 精確帳務系統
- **擬真交易成本**：模擬交易包含 **0.1425% 手續費 (低消 1 元)** 與 **0.1% 證交稅**。
- **JSON 記帳**：所有模擬交易數據皆儲存於 `paper_wallets.json`，方便備份與查閱。

---

## 🛠️ 安裝與環境設定

### 1. 安裝依賴套件
確保已安裝 Python 3.10+，並執行：
```bash
pip install django djangorestframework yfinance pandas backtrader schedule
```

### 2. 初始化資料庫
```bash
python manage.py makemigrations
python manage.py migrate
```

### 3. 設定專案名稱
請確保 `monitor_top10.py` 中的專案名稱設定正確（根據你的資料夾結構）：
```python
# monitor_top10.py
PROJECT_NAME = 'quant_platform'  # 依據你的資料夾名稱修改
```

### 4. Email 設定
請至 `utils/emailer.py` 設定你的 Gmail SMTP 資訊，以啟用通知功能。

---

## 🚀 使用說明

### 模式一：桌面監控 (日常掛機用)
這是最常用的模式，適合每日開盤期間掛在背景執行。
```bash
python gui_monitor.py
```
* 點擊 **「▶ 啟動每日排程」** 即可縮小視窗掛機。
* 系統會在 **13:40** 自動掃描訊號、更新 Top 10 排行榜、並寄送戰報。

### 模式二：網頁戰情室 (分析回測用)
適合需要查看詳細圖表或排行榜時使用。
```bash
python manage.py runserver
```
* 打開瀏覽器訪問 `http://127.0.0.1:8000/`。
* 可查看策略競技場的即時排名。

---

## 📂 專案結構

```text
stockAlgoTrading/
│
├── gui_monitor.py          # [入口] 桌面監控主程式 (Tkinter)
├── monitor_top10.py        # [核心] 策略競技場後台腳本
├── manage.py               # Django 管理腳本
├── paper_wallets.json      # [數據] 模擬交易帳本
│
├── quant_platform/         # Django 專案設定檔 (settings.py 在此)
│
├── backtester/             # [APP] 回測核心模組
│   ├── models.py           # 資料庫模型 (股價、競技場紀錄)
│   ├── views.py            # API 介面 (前後端溝通)
│   ├── engine/runner.py    # Backtrader 回測引擎
│   └── templates/          # 前端 HTML 頁面
│
└── utils/                  # [工具]
    ├── emailer.py          # Email 發送模組
    └── paper_trader.py     # 模擬錢包記帳模組
```

---

## 🤖 開機自動啟動 (Windows)

若要實現「開機即自動掛機」，請依照以下步驟：

1.  在專案目錄下建立 `auto_start.bat`：
    ```batch
    @echo off
    cd /d C:\stockAlgoTrading
    python gui_monitor.py
    exit
    ```
2.  按 `Win + R`，輸入 `shell:startup`。
3.  將 `auto_start.bat` 的 **捷徑** 貼到啟動資料夾中。
4.  確保 `gui_monitor.py` 程式碼中包含 `self.start_schedule()` 以自動啟動排程。

---

## ⚠️ 免責聲明

本專案僅供 **程式交易研究與學術用途**。
系統提供的所有交易訊號、回測結果與模擬績效，皆不構成任何投資建議。使用者應自行承擔真實交易的所有風險。

> **Happy Coding & Trading! 🚀**